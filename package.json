{
  "name": "num-ninjas",
  "private": true,
  "scripts": {
    "dev:bot": "pnpm --filter bot dev",
    "build:bot": "pnpm --filter bot build",
    "build:web": "pnpm --filter web build",
    "db:generate": "pnpm --filter @numninja/database db:generate",
    "db:push": "pnpm --filter @numninja/database db:push",
    "db:seed": "pnpm --filter @numninja/database db:seed",
    "db:reset": "pnpm --filter @numninja/database db:reset"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/engines",
      "better-sqlite3",
      "esbuild",
      "prisma"
    ]
  }
}
